import React from "react";

import "./Toolbar.scss";

const Toolbar = ({ paletteRef }) => {
    
    return(
        <div className="toolbar-container" ref={paletteRef}>

        </div>
    );
};

export default Toolbar;