import React from "react";

import "./Toolbar.scss";

const Toolbar = () => {
    return(
        <div className="toolbar-container">
            Toolbar
        </div>
    );
};

export default Toolbar;